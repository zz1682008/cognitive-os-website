# 官网 R5：信号、对话与整体协同

## Goal / Constraints / Done when

老板本轮明确要求撤掉三处写实大脑，以鼠标响应的抽象脑电波/科技点替代首屏；客服保留标题，以几组同问不同答直接表现自然、懂业务；企业协同成为一体化图形，去掉方框拼接和所有演示状态碎字。只处理本轮公开页面，不改功能进度。

- Design Read：面向企业负责人的 AI 产品官网，沿用灰白/石墨/品牌橙与现有字体，采用抽象信号的计算视觉和直观对话，保持已有页面顺序与工作台。`DESIGN_VARIANCE=8 / MOTION_INTENSITY=8 / VISUAL_DENSITY=2`。
- 审视 R4：保留整体介绍、下载入口、财务/法务不同的结果动画、秘书台的三栏结构和大幅品牌；移除三处写实脑图、资料方框连线、客服内部原理图、场景状态文字与客服接入脚注。
- 本轮核心是原生可交互图形，按老板要求用 Canvas 波形/粒子与同形静态后备，不再生成位图脑素材；保留已有企业文件夹图片。原生 B 端小型展示与示例计算不变，不新增第三方依赖或业务执行器。
- 首屏信号随指针产生局部形变；企业协同使用同一视觉语言构成一个统一流场，资料与四模块名称融入其中，无方框。客服三例：加急分批交付、发票抬头更正、设备报错急用；均为虚构产品场景，不是真实客户记录或实际服务承诺。
- 清除所有“交互演示 / 即将演示 / 正在演示 / 演示已完成 / 知识与理解”和各场景播放状态栏；只保留一个页面级无文字暂停按钮满足持续动画的可访问性。保留工作内容必须的金额、风险类型、文件名和建议待确认，不新增可见免责声明。
- L0 §3/4/6/13：Website 只负责公开呈现和既有账号入口；无模型调用、知识/事实写者、企业系统连接或真实数据。版本配置、OIDC、B 端、后台、主线、其他任务、安装包和部署均不在范围内。
- 当前主线 clean，HEAD `3ce74ef830545d6a7ce329831659158715f8f8fa`。继续官网专属工作树 `worktrees/website/v1-redesign-20260905`，保留 R1–R4 历史素材与文档。仅用本任务 3011 预览，其他运行任务只作冲突输入。
- 新任务 inspect 为 `PREPARE_NEW_TASK_ADMISSION`，无 blockers。现有 intake：`TASK-E5C35D8C10A3 / SINGLE_TASK / ordinary_change / LIGHTWEIGHT / autonomous`，contract/dispatch ready，无风险项。价值 `1-3x / ONE_PASS`，仅完成当前视觉修正，停止于老板视觉验收前，不推进其他工作。
- 预期 9 条路径：`app/components/xeliti-neural-visual.tsx`、`app/components/xeliti-product-stories.tsx`、`app/components/xeliti-motion.tsx`、`app/globals.css`、`app/layout.tsx`、`app/page.tsx`、`public/og-business-r5.webp`、`README.md`、本记录。无新服务、公共 API 或运行拓扑变化。
- 低风险自实施与自查，不冒充独立复核。普通测试、构建、TypeScript、实页指针/暂停/响应式/reduced-motion 验证与 Lighthouse；不新增任务验收脚本。失败时静态内容完整呈现；本地候选可回退，不提交/推送/部署，不写进度台账。

## 当前结果

本地候选已实现并完成自查，等待老板视觉验收；未独立复核、未合并、未推送、未部署，不代表任何产品能力已发布。

- 首屏：原生 Canvas 信号波、细线和粒子。真实指针产生局部牵引与高亮；没有写实脑图片。协同段使用同一套流线汇聚成一个整体，模块名直接融入图形，不再用方框。移动端与 reduced-motion 静态呈现，离屏/后台停止更新，DPR 封顶、约 30 fps，不逐帧更新 React 状态。
- 客服：保留原主题，三组问题直接对照模板回答与理解订单、开票和售后上下文的自然回答。各组滚动进入后依次呈现问题、模板回答、AI 回答，不要求点击切换；虚构助手仅保留小头像，不出现大脑或内部知识原理图。
- 删除各场景状态栏、资料图说明和客服接入脚注。可见正文检索“交互演示 / 正在演示 / 演示已完成 / 知识与理解 / 让现有电话”均为零；首页脑图引用为零。仅保留一个无文字的页面级暂停图标，减少动态效果和手机模式下隐藏。
- 实际本轮修改 8 条预期路径；`app/page.tsx` 复用既有结构，本轮无需再次修改。`public/og-business-r5.webp` 来自最终首页真实渲染，1320 × 704、46,046 bytes；分享图请求 200。R1–R4 历史文件保留，不删除。

### 验证

- `npm test`：7/7 普通测试、ESLint 和生产构建通过。`npx tsc --noEmit` 与 `git diff --check` 通过。生成分享图后再次生产构建通过。
- 真实浏览器：1440 × 1000、390 × 844、320 × 780 检查；首屏/客服/协同无横向溢出。亮色、深色手机和桌面均检查；Canvas 2 个、旧脑图引用 0 个。
- 指针确实改变波形；点击暂停后的两张 Canvas 区域截图逐字节相同，恢复播放后画面改变。运行中切换 reduced-motion 后场景动画属性清除、客服隐藏元素为 0，所有金额与计数恢复最终值。禁用脚本后 SVG 波形和正文仍完整显示。
- 下载弹窗打开、Escape 关闭并归还焦点通过；版本配置未改，未配置地址时不制造下载链接。手机导航打开、点击客服后关闭、Escape 关闭并归还焦点通过；Personal 路由跟随重定向后 200。
- Lighthouse 桌面：Performance 99 / Accessibility 100 / Best Practices 96 / SEO 100，LCP 0.7 s、TBT 0 ms、CLS 0。手机：82 / 100 / 96 / 100，LCP 3.8 s、TBT 70 ms、CLS 0。手机首屏加载仍有优化空间，不声称满足 LCP < 2.5 s。两份报告唯一浏览器控制台错误是既有 `127.0.0.1:5200/.well-known/openid-configuration` 连接拒绝；本轮不启动或修改其他任务的账号服务。
- 截图与 Lighthouse 原始文件：`/tmp/xeliti-r5-visual.SLA7kK/`。这是本机临时检查产物，不是发布证据。
- 收尾复核官网主线依旧 clean、HEAD 未变；依赖、下载配置和 Sites 配置未改。本次只重启自己的 3011 生产预览，其他服务未触碰。

预览：`http://127.0.0.1:3011/?revision=r5-deliverable`。下一步只等待老板检查视觉，不开启新工作。
