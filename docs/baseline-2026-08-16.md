# MASSOS Website source baseline — 2026-08-16

## Scope

- Existing MASSOS public website source only.
- No page, copy, authentication, hosting or deployment behavior was changed for this baseline.
- `.openai/hosting.json` is retained with no D1 or R2 binding.
- The repository contains no user account authority and no login implementation at this baseline.

## Verification before the first commit

- Installed exactly from `package-lock.json` with `npm ci`.
- `npm test` passed: ESLint completed and the vinext production build completed.
- Build routes: `/`, `/personal`, `/robots.txt` and `/sitemap.xml`.
- A targeted scan found no private-key, common cloud access-key, GitHub token, OpenAI-style key or Slack token pattern in tracked source candidates.
- `npm ci` reported 16 dependency advisories (2 low, 3 moderate, 11 high). This baseline records rather than auto-fixes them because `npm audit fix --force` may change the dependency graph and is outside the source-pinning step. Dependency remediation must be reviewed separately before public release.

## Delivery boundary

- This file and the initial repository commit make the Website an immutable input for CARD-499.
- This is local source evidence only. It is not a saved Sites version, preview deployment, public deployment or release.
