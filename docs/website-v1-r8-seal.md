# 官网 V1.0 宣传版 · R8 设计封板

## Goal / Constraints / Done when

- 老板原话：「好，把这一版定下来，然后直接发到那个线上」「别忘了封板，Git」。R8 视觉设计已获老板接受，不再继续改版。
- 本片固定已验收的独立 Website 候选：提交累积 R1–R8 文件，打唯一非生产标签，推送既有官网 origin。源码及交互不作新修改；完成条件是分支和标签的远端 SHA 与本地封板提交一致。此处不合并 main，不作服务器写入；发布片另行按正式入口执行。
- Intake：`TASK-FBCC6E879A50 / SINGLE_TASK / ordinary_change / LIGHTWEIGHT / autonomous`，风险为空。这是可逆的已验收设计 Git 封存，不改变发布流程、认证或部署权限。依 Workflow §3 分开 Git 定稿与高风险部署，不以本片代替发布授权及验收。
- 范围：官网既有 40 条候选路径，更新其中 README，新增本记录，共 41 条。其他任务的仓库、工作树、进程、根台账均不触碰。保留旧版文档和未使用的历史图片，不改写过去的候选验收结果。
- L0 §3/4/6/13：仅公开营销展示及其有界客户端状态；不引入服务、企业数据写入或身份权威。宣传中的推演及 AI 办公是产品设计示意，不代表后端能力或正式版本进度。

## Git 版本

- 基线：`3ce74ef830545d6a7ce329831659158715f8f8fa`，官网本地 main 与本次封板前的远端 main 一致。
- 定稿分支：`codex/website-v1-redesign-20260905`。
- 设计封板标签：`website-v1.0-r8-20260905`。该标签是设计源码封板，不是 `prod` 标签。
- 精确提交以 `git rev-parse 'website-v1.0-r8-20260905^{commit}'` 为准；远端以 `git ls-remote origin 'refs/tags/website-v1.0-r8-20260905*'` 核验。标签只创建一次，不移动、不覆盖。
- 历史 main 留作源码回退参考；线上回滚必须使用发布前实测的上一部署目录，不假定 main 就是线上版本。

## 已有验证及边界

- R8 定稿前 `npm test`：9 项普通测试、lint、生产构建通过；TypeScript 与 diff 检查通过。真实浏览器已检查滚动的理解、查阅、安排、回复各阶段，以及重播、暂停、离屏、桌面亮暗色、手机窄屏、减少动态效果及无脚本后备。具体记录见 `website-service-r8.md`。
- 本次 Git 封板新增/更新文档，并清除首次暂存检查发现的产品场景文件一处空行尾随空格，不改变页面内容或行为。普通单元测试 9/9、lint、TypeScript 复跑通过；完整暂存范围通过 diff 检查后提交，不以封板检查冒充线上发布测试。
- 已知性能边界：R8 本地 Lighthouse 桌面 97 / 100 / 96 / 100，移动 85 / 100 / 96 / 100；移动 LCP 3.4 秒，未达到 2.5 秒目标。老板的设计定稿不被解释为对所有性能指标的豁免。
- macOS 与 Windows 安装包 URL 仍未配置；保留下载入口及明确的不可下载状态，不伪造安装包。统一账号入口协议未改；本地账号中心不可达，不能据此宣称线上登录已验证。
- 自查不是独立复核；本记录不替代高风险发布所需独立复核和真实线上验收。

## 发布状态

尚未部署。现有发布文档登记的官网是测试域名 `https://twww.linzhaozhao.com`，需要老板确认本次指该地址还是另一个正式官网域名后，才允许服务器写入。继续沿用既有服务器路线，不使用老板已否决的 Sites 托管。

发布前另行固定已接受主线 SHA，执行既有跨仓库 S3 基线门禁，确认本次静态构件、目标域名、账号来源和上一版回滚路径。正式生产发布还须满足当前安全门禁；封板标签不能证明测试环境通过、生产债务关闭或部署完成。后续发布结果另追加记录，不回写 R1–R8 的历史候选结论。
