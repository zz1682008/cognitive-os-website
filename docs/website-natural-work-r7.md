# 官网 R7：一句话交代，理解后办好

## Goal / Constraints / Done when

老板在 R6 制作途中扩大官网展示要求，先保留当前财务候选，再重新 intake。不是正式财务/法务/客服功能实施，也不接受或推进其他开发任务。

- Design Read：企业 AI 科技官网定向重构，保留灰白/石墨/品牌橙、字体、窗口与首屏信号。财务/法务采用双轮对话叙事；客服以手机和文档探索展示处理过程；企业协同用抽象点阵大脑与资料翻页融成一个场景。`DESIGN_VARIANCE=8 / MOTION_INTENSITY=8 / VISUAL_DENSITY=2`。
- 财务：企业文件夹里的工作台。自然语言录入凭证，随后再发一句生成资产负债表和利润表，分别下载合成示例 CSV。借贷、月度结果与资产平衡一致。
- 法务：审阅销售发来的合同，定位条款风险；继续用自然语言拟定合同，生成草案。保留需要法务确认的边界，不宣称机器完成正式审批或法律判断。
- 客服：同一句问题进入两部手机；传统端模板匹配，AI 端动态查阅公司资料、结构化信息再回答。展示的是文件依据处理，不伪造真实模型内部思维或真实连接。
- 企业协同：非写实、无皮肤/器官纹理的点阵脑，资料翻动后汇入，财务/法务/客服/秘书的结果通过流动信号出现，少字、整体呈现，不用孤立方框连接文字标签。原生 Canvas/CSS/现有 GSAP，不生成写实大脑图片，不新增依赖。
- 确保滚动自动展示，无需点击切页；动效暂停、离屏停止、手机与 reduced-motion 静态完整后备。发送按钮只重放固定示例，不接收企业数据。
- L0 §3/4/6/13 只涉及 Website 公开呈现，无事实、知识、身份、执行写者，无真实模型或企业接口。没有 PRD/正式产品语义变化，不越过尚未建立的财务/法务执行层。
- 官网 main clean、HEAD `3ce74ef830545d6a7ce329831659158715f8f8fa`。保留 R1–R6 候选历史，复用 `codex/website-v1-redesign-20260905`。只使用自己的 3011 预览；并行 Root/B 端任务不在本轮权限内。
- 价值 `1-3x / ONE_PASS`，老板明确追加要求作为边界确认；单轮本地候选，停于老板视觉验收，不合并、不推送、不部署、不写项目台账。低风险自实施、自查，不冒充独立复核。
- 原 R6 8 条路径包含其记录；扩展后本轮预期 10 条路径（继续累计财务工作）：`app/components/xeliti-product-stories.tsx`、`app/components/xeliti-motion.tsx`、`app/components/xeliti-neural-visual.tsx`、`app/globals.css`、`app/lib/website-demo.mjs`、`app/lib/website-demo.d.mts`、`test/website-demo.test.mjs`、`README.md`、`docs/website-finance-r6.md`、本记录。页面路由、元数据、下载配置、安装包、服务不改。
- 扩展前的最终 intake：`TASK-3C637C733EA9 / SINGLE_TASK / ordinary_change / LIGHTWEIGHT / autonomous`，`contract_ready=true / dispatch_allowed=true / branch_required=true`，风险触发为空。仅使用现有 intake 字段，不修改路由器或工作流规则。
- 普通测试/生产构建/TypeScript、真实滚动/发送/报表下载、桌面/手机/深色/减少动态效果检查和 Lighthouse；不用专用验证工具。失败保留现场，仅修本轮范围，静态后备始终可读。

## 当前结果

已形成本地视觉候选，等待老板看页面；不是正式产品功能交付或发布。实际改动在上述 10 条路径内，工作树同时保留此前 R1–R5 的未提交改动和素材，本次没有清理或覆盖那些历史路径。

### 画面与实际操作

- 财务保留工作台壳和企业文件夹层级；第一轮发送办公费录入请求后生成借贷各 1,200 元的凭证，下一幅连续工作台继续发出报表请求，再展开资产负债表及利润表。桌面可点击发送重放，滚动自动开始，不需要点击才能看到下一幅。
- 法务两轮分别呈现审阅原合同、标记两项风险和修改建议，以及按公司模板生成合同草案。真实点击两轮发送，均观察到输入内容先出现、结果初始不可见，随后结果及条款完整出现，状态完成。页面不宣称完成正式法律审批。
- 客服的两部手机收到同一句“明天就要装，能先发一部分吗？”。真实滚动观察到传统回复已出现、AI 回复仍不可见时，第一份订单资料正在查阅；随后流程、库存依次进入，再给出交付回答。过程截图保留，不以最终截图代替过程验证。
- 企业协同为原生点阵脑、三层翻页资料、流动信号及结果片段的连续构图。无写实器官图。点阵、资料翻转与信号共用可暂停的动画时钟，桌面深色视口暂停后的两张完整截图逐字节相同，恢复后继续播放。此前以页面坐标误截到首屏的两张图片不作为暂停验收证据。
- 两份 CSV 通过真实浏览器点击下载，并收到 `Page.downloadProgress state=completed`：资产负债表示例 470 bytes，利润表示例 296 bytes。文件内容由普通测试验证；不读取或管理用户下载目录，也没有修改浏览器下载设置。高层下载等待事件曾超时，不当作下载失败或成功依据。

### 自查结果

- 最新生产代码 `npm test`：9 项普通测试通过，lint 无警告，生产构建成功；`npx tsc --noEmit`、`git diff --check` 通过。
- 桌面 1440 px、手机 390 px、窄屏 320 px：财务、法务及客服内容完整，没有页面横向溢出；桌面亮/暗色工作台、手机模型及点阵场景实际看图。移动端按传统手机、资料、AI 手机顺序自然阅读。
- `prefers-reduced-motion: reduce`：动画场景标记为 0、五份结果均可见、利润恢复 33.4、发送重放禁用。禁用 JavaScript 后重新加载：无动画标记、无 canvas 就绪标记、五份结果可见、两份下载链接仍在、窄屏无溢出。验证后已恢复脚本、系统主题、视口与正常预览。
- 本轮 Lighthouse 桌面：Performance 99 / Accessibility 100 / Best Practices 96 / SEO 100，LCP 0.7 s、TBT 0 ms、CLS 0；移动：82 / 100 / 96 / 100，LCP 3.9 s、TBT 60 ms、CLS 0。这次跑分在最后一次点阵排列微调之前，首屏和页面结构未变，不冒充最终源码再次跑分。移动 LCP 尚未达到设计技能的 2.5 s 目标，保留性能待优化项，不宣称所有性能门槛通过。
- 本地统一账号中心 `127.0.0.1:5200/.well-known/openid-configuration` 拒绝连接是既有环境问题；本轮没有启动、修复或变更账号服务，也不宣称登录联调通过。
- 截图及 Lighthouse 临时证据：`/tmp/xeliti-r7-visual.BKtcot/`，包括 `service-process.png`、`brain-dark-paused.png`、`service-mobile.png`、`legal-nojs-mobile.png` 及两份 Lighthouse JSON。临时目录不是长期发布产物。
- 结束前官网 main 仍 clean，HEAD `3ce74ef830545d6a7ce329831659158715f8f8fa`；未动其他任务，未提交、合并、推送、部署，未修改台账。此为自查，不是独立复核；老板视觉验收尚未发生。

本地预览：`http://127.0.0.1:3011/?revision=r7-deliverable#workbench`。停在本次候选，不自行扩展其他页面或正式产品功能。
